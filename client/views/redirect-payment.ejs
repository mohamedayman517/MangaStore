<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("./partials/meta") %>

    <title>Manga Store - Payment</title>
    <style>
      .loader {
        width: 130px;
        aspect-ratio: 1;
        border-radius: 50%;
        background: radial-gradient(farthest-side, #ffa516 94%, #0000) top/8px 8px no-repeat,
          conic-gradient(#0000 30%, #ffa516);
        -webkit-mask: radial-gradient(farthest-side, #0000 calc(100% - 8px), #000 0);
        animation: l13 1s infinite linear;
      }
      @keyframes l13 {
        100% {
          transform: rotate(1turn);
        }
      }
    </style>
    <link rel="stylesheet" href="/css/build.min.css" />
  </head>
  <body class="h-screen flex justify-center items-center w-full flex-col gap-6">
    <%- include("./partials/loader") %>
    <div id="text" style="z-index: 1001">Please wait while directing to payment page</div>
    <script src="/js/payment-redirect.js"></script>
    <script src="/js/dark-mode.js"></script>
  </body>
</html>
