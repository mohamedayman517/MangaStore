<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= typeof title !== 'undefined' ? title : 'Message' %></title>
  <link rel="stylesheet" href="/css/build.min.css" />
  <style> body { padding: 24px; } .card{max-width:720px;margin:0 auto} </style>
</head>
<body>
  <div class="card p-6 shadow rounded bg-white">
    <h1 class="text-2xl font-semibold mb-2"><%= typeof title !== 'undefined' ? title : 'Message' %></h1>
    <p class="text-gray-700"><%= typeof message !== 'undefined' ? message : '' %></p>
    <div class="mt-4">
      <a class="btn btn-primary" href="/">Return to home</a>
    </div>
  </div>
</body>
</html>
